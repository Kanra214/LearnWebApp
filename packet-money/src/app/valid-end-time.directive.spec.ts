import { ValidEndTimeDirective } from './valid-end-time.directive';

describe('ValidEndTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidEndTimeDirective();
    expect(directive).toBeTruthy();
  });
});
