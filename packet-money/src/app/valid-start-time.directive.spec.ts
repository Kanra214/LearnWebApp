import { ValidStartTimeDirective } from './valid-start-time.directive';

describe('ValidStartTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidStartTimeDirective();
    expect(directive).toBeTruthy();
  });
});
